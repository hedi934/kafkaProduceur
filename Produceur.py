import numpy as np
from kafka import *
import datetime
import json
import random
from datetime import datetime

# L'exercice est de cree un produceur qui envoie des donneés a kafka
# pour ce faire vous allez simuler le temps de visionnage d'un site par des utilisateur
# je veux que les données suivante soient envoyé :
# Id_user : nous allons simuler seulement 4 users avec cette nomenclature : user_i ( i allant de 1 a 4 )
# Id_page : nous allons simuler seulement 4 page avec cette nomenclature : page_i ( i allant de 1 a 4 )
# milliseconde : un random de milliseconde allant de 0 a 3 minutes
# date au format date ( avec heure,minute et seconde) pour voir quand l'evenement a ete produit


def add_datatokafka():


    while 1:


if __name__ == "__main__":
    add_datatokafka()